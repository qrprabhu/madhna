-- Run once in Supabase SQL Editor to create your first admin.
-- 1. Create the user in Dashboard → Authentication → Users → Add user (email + password).
-- 2. Copy that user's UUID from the users list.
-- 3. Replace YOUR_USER_UUID below and run this script.

-- insert into public.user_roles (user_id, role)
-- values ('YOUR_USER_UUID', 'admin');
